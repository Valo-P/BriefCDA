<!DOCTYPE html>
<html lang="fr">
<head>
    <%- include('../head'); %>
</head>
<body>
<%- include('../header'); -%>
<main class="container">
    <h1>List of all Categories</h1>
        <table>
            <% for(var i=0; i < categories.length; i++) { %>
                <tr>
                    <td>ID : <%= categories[i]._id %></td>
                    <td>/ Name : <%= categories[i].categoryName %></td>
                </tr>
            <% } %>
        </table>
</main>
<%- include('../footer'); -%>
</body>
</html>